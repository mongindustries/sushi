#pragma once

namespace sushi::core {

    /**
     * \brief The object in which all sushi classes inherit from.
     * 
     */
    class CoreObject {

    public:

        virtual ~CoreObject() = default;
    };
}
